import { AppselectdataDirective } from './appselectdata.directive';

describe('AppselectdataDirective', () => {
  it('should create an instance', () => {
    const directive = new AppselectdataDirective();
    expect(directive).toBeTruthy();
  });
});
